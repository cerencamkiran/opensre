schema_version: "1.0"
scenario_id: 002-connection-exhaustion
engine: postgres
engine_version: "15"
instance_class: db.r6g.2xlarge
region: us-east-1
db_instance_identifier: payments-prod
db_cluster: payments-cluster
failure_mode: connection_exhaustion
severity: critical
scenario_difficulty: 3
adversarial_signals:
  - CPUUtilization elevated 35-50% from idle session query load — looks like cpu_saturation
  - FreeStorageSpace stable but BinLogDiskUsage growing — decoy storage signal
available_evidence:
  - rds_metrics
  - rds_events
  - performance_insights
