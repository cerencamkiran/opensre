schema_version: "1.0"
scenario_id: 010-replication-lag-missing-metric
engine: postgres
engine_version: "15"
instance_class: db.r6g.2xlarge
region: us-east-1
db_instance_identifier: reporting-prod
db_cluster: reporting-cluster
failure_mode: replication_lag
severity: critical
scenario_difficulty: 3
adversarial_signals:
  - ReplicaLag metric absent — must infer from TransactionLogsGeneration and RDS events
  - CPUUtilization mildly elevated from WAL write load — could mislead toward cpu_saturation
available_evidence:
  - rds_metrics
  - rds_events
  - performance_insights
