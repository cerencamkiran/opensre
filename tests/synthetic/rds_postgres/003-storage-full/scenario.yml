schema_version: "1.0"
scenario_id: 003-storage-full
engine: postgres
engine_version: "15"
instance_class: db.r6g.xlarge
region: us-east-1
db_instance_identifier: orders-prod
failure_mode: storage_full
severity: critical
scenario_difficulty: 3
adversarial_signals:
  - CPUUtilization mildly elevated from I/O stall during bulk archival — could mislead toward cpu_saturation
  - SwapUsage elevated — looks like memory pressure
available_evidence:
  - rds_metrics
  - rds_events
  - performance_insights
