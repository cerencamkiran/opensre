schema_version: "1.0"
scenario_id: 008-storage-full-missing-metric
engine: postgres
engine_version: "15"
instance_class: db.r6g.large
region: eu-west-1
db_instance_identifier: billing-prod
failure_mode: storage_full
severity: critical
scenario_difficulty: 3
adversarial_signals:
  - FreeStorageSpace metric absent — must infer from events and write latency collapse
  - CPUUtilization elevated from I/O stall — could mislead toward cpu_saturation
available_evidence:
  - rds_metrics
  - rds_events
  - performance_insights
